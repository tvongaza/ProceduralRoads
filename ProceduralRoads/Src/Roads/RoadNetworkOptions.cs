namespace ProceduralRoads;

/// <summary>Island grouping shared by generation and diagnostics.</summary>
public sealed class RoadNetworkOptions
{
    public bool WalkableIslands { get; set; } = true;
}
